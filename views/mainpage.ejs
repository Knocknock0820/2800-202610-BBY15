<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('partials/head', { title: 'My Plants', pageCSS: '/css/mainpage.css' }) %>
</head>

<body>
  <!-- ===== HEADER ===== -->
  <%- include('partials/navbar', { showLogout: true, activeLink: 'home', showGuide: true }) %>

  <!-- ===== WEATHER STRIP ===== -->
  <div class="weather-strip">
    <span id="weatherText">Loading weather…</span>
  </div>

  <!-- ===== PLANT LIST ===== -->
  <main class="plants-main container mt-3">
    <h2 class="my-plants-title mb-1">Hello, <%= name %>! 👋</h2>
    <p class="text-muted mb-3">Here are your plants:</p>
    <div class="plant-list" id="plantList">
      <!-- Plant cards are injected here by main.js -->
    </div>
  </main>

  <!-- ===== FLOATING ADD BUTTON (FAB) ===== -->
  <button class="btn btn-primary rounded-circle shadow" id="btnAdd" title="Add plant" data-bs-toggle="modal"
    data-bs-target="#addPlantModal"
    style="position: fixed; bottom: 80px; left: 50%; transform: translateX(-50%); width: 56px; height: 56px; z-index: 1000; background-color: #687d31; border: none; display: flex; align-items: center; justify-content: center;">
    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor"
      stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
      <line x1="12" y1="5" x2="12" y2="19" />
      <line x1="5" y1="12" x2="19" y2="12" />
    </svg>
  </button>

  <!-- code adopted from boostrap : https://getbootstrap.com/docs/5.3/components/modal/
    Modified by: Harun Yaprak -->
  <!-- ===== ADD PLANT MODAL (Bootstrap native) ===== -->
  <div class="modal fade" id="addPlantModal" tabindex="-1" aria-labelledby="addPlantModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content" style="background-color: #f2f1ee; border-radius: 24px;">
        <div class="modal-header border-0 pb-0 mt-2">
          <h5 class="modal-title fw-bold mx-auto" id="addPlantModalLabel"
            style="color: #19350c; font-family: 'Playfair Display', serif; font-size: 1.3rem;">Add a New Plant</h5>
          <button type="button" class="btn-close position-absolute end-0 me-3" data-bs-dismiss="modal"
            aria-label="Close"></button>
        </div>
        <div class="modal-body px-4 pt-4">
          <div class="mb-3">
            <label for="plantSpecies" class="form-label fw-bold small mb-1"
              style="color: #687d31; text-transform: uppercase; letter-spacing: 0.05em;">Plant Species</label>
            <select class="form-select" id="plantSpecies"
              style="border-radius: 12px; padding: 12px 14px; border: 1.5px solid #d5d3cc; box-shadow: none;">
              <option value="" disabled selected>Loading species...</option>
            </select>
          </div>
          <div class="mb-3">
            <label for="plantName" class="form-label fw-bold small mb-1"
              style="color: #687d31; text-transform: uppercase; letter-spacing: 0.05em;">Nickname (Optional)</label>
            <input type="text" class="form-control" id="plantName" placeholder="e.g. My Favorite Fern" maxlength="40"
              style="border-radius: 12px; padding: 12px 14px; border: 1.5px solid #d5d3cc; box-shadow: none;" />
          </div>
        </div>
        <div class="modal-footer border-0 pt-0 px-4 pb-4 d-flex gap-2">
          <button type="button" class="btn flex-fill" data-bs-dismiss="modal"
            style="border: 1.5px solid #d5d3cc; border-radius: 12px; color: #687d31; font-weight: 600; padding: 12px;">Cancel</button>
          <button type="button" class="btn flex-fill" id="btnSave"
            style="background-color: #687d31; color: #d5d3cc; border-radius: 12px; font-weight: 600; padding: 12px; border: none;">Save
            Plant</button>
        </div>
      </div>
    </div>
  </div>

  <!-- User Guide Modal (Bootstrap native) -->
  <div class="modal fade" id="userGuideModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content" style="background-color: #d5d3cc; border-radius: 16px; border: 2px solid #687d31;">
        <div class="modal-header border-0 pb-0">
          <h5 class="modal-title fw-bold" style="color: #19350c; font-family: 'Playfair Display', serif;">User Guide
          </h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body text-center">
          <div class="guide-photo-placeholder mb-3 shadow-sm">
            <img id="guidePhotoDisplay" src="/images/guide_step1.png" alt="User Guide Step"
              onerror="this.src='/images/error.png';">
          </div>

          <p id="guideDescription" class="mb-4" style="color: #19350c; font-weight: 500; min-height: 3em;">
            Welcome to Sprout! We are excited to help you on your plant care journey. This quick guide will show you how
            to make the most of our app and keep your green friends thriving!
          </p>

          <div class="d-flex justify-content-between px-2">
            <button class="btn" onclick="navigateGuide(-1)"
              style="border: 1.5px solid #687d31; color: #687d31; border-radius: 10px; font-weight: 600;">Previous</button>
            <button class="btn" onclick="navigateGuide(1)"
              style="background-color: #687d31; color: #d5d3cc; border-radius: 10px; font-weight: 600; border: none;">Next</button>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- ===== BOTTOM NAVIGATION BAR ===== -->
  <%- include('partials/footer', { showBottomNav: true, activeTab: 'home' }) %>

  <!-- External JS file — all client-side logic lives here -->
  <script src="/js/main.js"></script>

  <% if (locals.firstLogin) { %>
    <script>
      document.addEventListener("DOMContentLoaded", function () {
        var userGuideModal = new bootstrap.Modal(document.getElementById('userGuideModal'));
        userGuideModal.show();

        // Remove the query parameter from the URL to prevent showing it again on refresh
        const url = new URL(window.location);
        url.searchParams.delete('firstLogin');
        window.history.replaceState({}, '', url);
      });
    </script>
  <% } %>
</body>

</html>